# EJS + Tailwind with Express Demo Project
This includes everything needed to quickly get started on a Express EJS with Tailwind.

To use it, simply clone this repo with git clone (or just manually copying it) into an empty folder
and writing "npm install" to install all the needed dependencies.